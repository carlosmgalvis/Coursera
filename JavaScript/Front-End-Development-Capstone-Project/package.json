{
  "name": "stayhealthy-ibm-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install": "npm --prefix client install && npm --prefix server install",
    "client": "npm --prefix client run dev",
    "server": "npm --prefix server run dev",
    "start": "concurrently \"npm run client\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
